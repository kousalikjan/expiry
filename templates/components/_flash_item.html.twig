<div class="flex p-3.5 rounded-xl gap-8 justify-between items-center {{ color }}" {{ stimulus_controller('toast-hide') }} data-turbo-temporary>
    <span>{{ message|trans }}</span>
    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6" {{ stimulus_action('toast-hide', 'close', 'click') }}>
        <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
    </svg>
</div>